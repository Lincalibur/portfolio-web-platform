# Leave empty so the Vite dev server proxies /health and /api to the API (avoids HTTPS cert errors).
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://localhost:5180
VITE_ENABLE_INTERACTION_METRICS=true
VITE_USE_HOST_STATS_API=true
