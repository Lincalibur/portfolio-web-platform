{
  "title": "Script Repository",
  "description": "Run cinematic automation demos — ASCII banners, animated progress, radar scans, rocket launches, and trust-score finales.",
  "scripts": [
    {
      "id": "asset_discovery",
      "filename": "asset_discovery.py",
      "title": "The Reconnaissance Tool",
      "tagline": "Modular passive reconnaissance & asset mapping",
      "description": "Enter a target domain and watch a radar scan map your digital footprint with animated progress and a live results tree.",
      "highlights": [
        "ASCII radar scan",
        "Animated progress bar",
        "Live endpoint tree"
      ],
      "language": "python",
      "sourcePath": "/fixtures/scripts/asset_discovery.py",
      "delayMs": 120,
      "inputs": [
        {
          "id": "target",
          "label": "Target domain",
          "type": "text",
          "default": "portfolio.local",
          "placeholder": "example.com"
        }
      ]
    },
    {
      "id": "deploy_env",
      "filename": "deploy_env.sh",
      "title": "The DevOps Framework",
      "tagline": "Launch sequence automation",
      "description": "Pick an environment and follow a staged launch — progress bars for each phase, then a rocket lift-off finale when everything is healthy.",
      "highlights": [
        "Multi-phase progress UI",
        "Rocket ASCII animation",
        "Launch complete finale"
      ],
      "language": "bash",
      "sourcePath": "/fixtures/scripts/deploy_env.sh",
      "delayMs": 110,
      "inputs": [
        {
          "id": "environment",
          "label": "Environment",
          "type": "select",
          "default": "staging",
          "options": [
            { "value": "dev", "label": "Development" },
            { "value": "staging", "label": "Staging" },
            { "value": "production", "label": "Production" }
          ]
        }
      ]
    },
    {
      "id": "compliance_check",
      "filename": "compliance_check.yml",
      "title": "The Risk & Compliance Audit",
      "tagline": "Trust & safety scorecard",
      "description": "Run a visual compliance audit with checklist trees and a framed trust score — stricter baselines can surface review warnings.",
      "highlights": [
        "npm-style success tree",
        "Trust score panel",
        "Sparkle finale on pass"
      ],
      "language": "yaml",
      "sourcePath": "/fixtures/scripts/compliance_check.yml",
      "delayMs": 130,
      "inputs": [
        {
          "id": "inventory",
          "label": "Inventory group",
          "type": "select",
          "default": "application_servers",
          "options": [
            { "value": "application_servers", "label": "Application servers" },
            { "value": "edge_nodes", "label": "Edge nodes" },
            { "value": "data_plane", "label": "Data plane" }
          ]
        },
        {
          "id": "baseline",
          "label": "Compliance baseline",
          "type": "select",
          "default": "cis_level1",
          "options": [
            { "value": "cis_level1", "label": "CIS Level 1" },
            { "value": "pci_dss", "label": "PCI-DSS" },
            { "value": "custom_hard", "label": "Custom hardening" }
          ]
        }
      ]
    }
  ]
}
